# Image Processing
1. parcer.c 是一个解析程序
2. invert 是图像反相，新像素值= 255 - 旧像素值
3. gray 是把彩色图像变为灰度图，由gray = （r + g + b）/3 得到
每个像素点灰度图下的值。
4. olor_overlay 是计算两个图片重合后颜色的变换
5. crop 是图像的裁剪
6. gaussion blur 是一种模糊手段，通过计算像素点及其邻域像素的加权平均值实现
7. sobel 是一种边缘检测手段，通过分别计算图像在水平和垂直方向的梯度值，结合梯度幅值与方向判断像素点是否为边缘。
